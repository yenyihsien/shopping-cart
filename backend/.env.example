# Server
PORT=8787
NODE_ENV=development
BCRYPT_SALT_ROUNDS=12

# AI provider key (never commit real key)
OPENAI_API_KEY=replace_with_real_key

# Optional promo logic seed
PROMO_RULE_SALT=replace_with_random_secret
